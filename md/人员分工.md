# 变电站巡检仿真系统 — 人员分工

> 基准文档：参考架构plan_all_v3.md (v3.3)  
> 日期：2026-06-03  
> 团队：4 人

---

## 一、模块依赖关系

```
common（公共基础层）
  ↑
  ├── controller（核心调度器）      ← Person A
  ├── car（小车知识源）×N           ← Person B
  ├── navigator（路径规划器）       ← Person C
  ├── target-planner（目标规划器）   ← Person C
  ├── task-configurator（任务配置器）← Person C
  └── display（展示层）             ← Person D
        ↑
      launcher（一键启动）          ← Person D
```

启动依赖：common 必须先完成；其他模块依赖 common，但彼此之间不依赖。

---

## 二、分工总览

| 人员 | 负责模块 | 难度 | 核心挑战 |
|------|----------|------|----------|
| **Person A** | common + controller | ⭐⭐⭐⭐⭐ | 公共库设计（所有人依赖）+ 节拍驱动调度器（系统大脑） |
| **Person B** | car | ⭐⭐⭐⭐ | 5状态机 + 原子移动 + 分布式锁 + History + mapHeat |
| **Person C** | navigator + target-planner + task-configurator + 动态障碍物 | ⭐⭐⭐⭐ | BFS/A* 路径搜索 + 贪心分配 + 黑板初始化 + 动态障碍 |
| **Person D** | display + launcher | ⭐⭐⭐⭐ | WebSocket 双向通信 + Canvas 渲染 + 一键启动集成 |

---

## 三、Person A：common + controller

### 为什么这样分配

common 是所有人依赖的公共库，controller 是唯一调度器，这两个模块天然需要一个人同时掌控，否则接口定义来回沟通成本太高。

### 3.1 common 模块交付清单

| 文件 | 职责 |
|------|------|
| `common/Point.java` | 坐标类（x, y）+ 曼哈顿距离 + JSON 序列化 |
| `common/CarStatus.java` | 5 状态枚举（IDLE/WAITING_ROUTE/READY/MOVING/BLOCKED）+ 颜色 + 中文名 |
| `common/AlgorithmType.java` | BFS/ASTAR 枚举 |
| `common/RouteStep.java` | 路径步骤（position + stepIndex） |
| `common/SimulationState.java` | 全局状态快照（含内部类 CarInfo），WebSocketBridge 每节拍构建前端 JSON 的数据载体 |
| `common/BlackboardClient.java` | Redis 封装，所有 key 的全部读写方法（含 mapHeat 热力图计数器、controller:instance 单实例锁） |
| `common/DistributedLock.java` | Redis 分布式锁（SETNX + Lua 释放，超时 5s） |
| `common/MessageBus.java` | RabbitMQ 封装（声明队列、发布、订阅、连接管理） |
| `common/MessageTypes.java` | 所有消息类型常量 |
| `common/MessageBuilder.java` | 统一消息 JSON 构建工具（fastjson2） |

**对外接口（供 B/C/D 使用）**：

```java
// BlackboardClient — 所有知识源通过它读写 Redis
BlackboardClient bb = new BlackboardClient("localhost", 6379);
bb.setCarStatus("Car001", CarStatus.IDLE);
Point pos = bb.getCarPosition("Car001");

// DistributedLock — Car 移动时使用
DistributedLock lock = new DistributedLock(jedis, "lock:Car001");
if (lock.tryLock(5000)) { ... lock.unlock(); }

// MessageBus — 所有模块通过它收发 MQ 消息
MessageBus mb = new MessageBus("localhost", 5672, "guest", "guest");
mb.declareCarQueue("Car001");
mb.subscribe("Car_Car001", msg -> handleMessage(msg));
mb.publish("ControllerCmd", messageJson);
```

### 3.2 controller 模块交付清单

> ⚠️ **任务书约束**：控制器必须独立运行，且在操作系统中只能运行一个实例。ControllerMain 启动时需通过 Redis SETNX `controller:instance` 互斥锁确保单实例。

| 文件 | 职责 |
|------|------|
| `controller/ControllerMain.java` | 入口：连接 Redis + RabbitMQ，检测单实例（SETNX `controller:instance`），启动 TickScheduler |
| `controller/TickScheduler.java` | 定时器驱动 tick() 循环，默认 500ms 间隔 |
| `controller/StatusDispatcher.java` | 遍历所有已注册车辆（KEYS Car*:Status），按状态分发命令（核心调度逻辑） |
| `controller/CommandHandler.java` | 订阅 ControllerCmd，处理知识源回复（TARGET_ASSIGNED / ROUTE_PLANNED / MOVED / BLOCKED / ROUTE_DONE / TASK_READY）+ Web 命令转发（SET_CONFIG / RESET / TOGGLE_PAUSE / SET_TICK_INTERVAL） |

**Controller 附加功能**（阶段3实现）：

| 功能 | 实现要点 |
|------|----------|
| 暂停/继续 | TickScheduler 加 `paused` 标志；收到 TOGGLE_PAUSE 后翻转；paused 时 tick() 跳过 |
| 节拍调速 | 收到 SET_TICK_INTERVAL 后重建 ScheduledExecutorService |
| 动态障碍物 | Controller 在 tick() 中每 N 拍调用 `DynamicObstacleUtil.generateDynamicObstacles()`（Person C 已实现，放 common） |
| 耗时统计 | tick=1 记录开始时间；探索率达标时计算耗时写入 TaskConfig |

**Controller 对外的接口（MQ 消息）**：

```
发送队列：
  → TargetPlannerCmd: ASSIGN_TARGET {carId, tick}
  → NavigatorCmd:     PLAN_ROUTE {carId, start{x,y}, target{x,y}, algorithm, tick}
  → Car_CarID:        TICK_MOVE {tick} | BLOCKED_TIMEOUT {carId}
  → TaskConfigCmd:    FORWARD_CONFIG {config...} | FORWARD_RESET {}
  → UpdateView(fanout): REFRESH_ALL {tick, explorationRate}

接收队列：
  ← ControllerCmd: TARGET_ASSIGNED | ROUTE_PLANNED | MOVED | BLOCKED | ROUTE_DONE | TASK_READY | SET_CONFIG | RESET
```

### 3.3 开发顺序

1. **common 全部**（必须最先完成，交付给 B/C/D）
2. ControllerMain + CommandHandler（先能启动 + 接收消息）
3. TickScheduler + StatusDispatcher（核心调度逻辑）
4. 与 B/C/D 联调

---

## 四、Person B：car

### 为什么这样分配

car 是纯执行端——收到 TICK_MOVE 命令后执行移动、更新状态、点亮地图。5 状态机 + 原子移动操作是整个系统逻辑最密集的代码，加上分布式锁、History 路径记录、mapHeat 热力图计数器，工作量充足。

### 4.1 car 模块交付清单

| 文件 | 职责 |
|------|------|
| `car/CarMain.java` | 入口：接受 carId 命令行参数；连接 Redis + RabbitMQ，订阅 Car_CarID，创建 CarAgent |
| `car/CarAgent.java` | 消息分发：收到 TICK_MOVE → 调用 MoveExecutor |
| `car/MoveExecutor.java` | 8 步原子移动操作（加锁→读状态→peek路径→检查障碍→pop路径→更新位置→点亮3×3→写状态→解锁）+ 每步写 `CarID:History`（RPUSH `{x,y,tick}`） + 点亮时 `INCR mapHeat` 热力图计数器 |

**Car 对外的接口（MQ 消息）**：

```
接收队列：
  ← Car_CarID: TICK_MOVE {tick} | BLOCKED_TIMEOUT {carId}

发送队列：
  → ControllerCmd: MOVED {carId, newPosition{x,y}, routeRemaining} 
  → ControllerCmd: ROUTE_DONE {carId, finalPosition{x,y}}
  → ControllerCmd: BLOCKED {carId, blockedPosition{x,y}, blockedTick}
```

**Car 内部状态转换规则**（只写自己管辖的变迁）：

```
READY → MOVING   收到 TICK_MOVE，下一步无障碍
MOVING → READY   移动成功，路径仍有剩余
MOVING → IDLE    路径走完，发 ROUTE_DONE
MOVING → BLOCKED 下一步有障碍，发 BLOCKED
(初始化时) → IDLE  注册到黑板
```

> ⚠️ Car 永不写 WAITING_ROUTE（那是 Controller 写的）

**CarAgent 收到 BLOCKED_TIMEOUT 时的处理**：

> Controller 在 BLOCKED 超时 ≥2 节拍后会清空该车的 RouteList、Target、BlockedTick，写 Status=IDLE，并发送 BLOCKED_TIMEOUT。
> Car 收到后：
> 1. 日志 WARN：`[CarID] 收到 BLOCKED_TIMEOUT，阻塞超时已由 Controller 处理`
> 2. 从 Redis 读取自身 Status：若早已为 IDLE，说明 Controller 已清理完毕
> 3. 不做任何黑板写入（Controller 已完成全部清理）
> 4. 等待下一轮从 IDLE 重新分配目标

> 📌 **动态添加小车**：CarMain 接受 carId 参数（如 `java -jar car.jar Car006`），启动即自动注册到黑板。Controller 通过 Redis `KEYS Car*:Status` 动态发现已注册车辆，新进程启动即加入，无需改配置。

### 4.3 开发顺序

1. CarMain + 基本消息收发
2. MoveExecutor（核心：8 步原子操作 + 分布式锁）
3. CarAgent（状态机逻辑 + BLOCKED_TIMEOUT 处理）
4. History 路径记录 + mapHeat 热力图计数器
5. 与 Person A 的 Controller 联调

---

## 五、Person C：navigator + target-planner + task-configurator

### 为什么这样分配

navigator、target-planner、task-configurator 三个模块都属于"规划层"：一个决定**去哪**（目标分配）、一个决定**怎么去**（路径搜索）、一个负责**地图初始化**。三者共享对 mapView/mapBlock 的理解，交给同一个人逻辑连贯。

### 5.1 navigator 模块交付清单

| 文件 | 职责 |
|------|------|
| `navigator/NavigatorMain.java` | 入口：订阅 NavigatorCmd |
| `navigator/PathPlanner.java` | 接口：`List<Point> plan(Point start, Point target, BlackboardClient bb)` |
| `navigator/BfsPathFinder.java` | BFS 实现（4 方向，避开障碍+车辆，边界检查） |
| `navigator/AStarPathFinder.java` | A* 实现（曼哈顿距离启发，PriorityQueue 开放列表） |
| `navigator/PathPlannerFactory.java` | 工厂：根据 algorithm 参数返回 BFS 或 A* 实例 |

**Navigator 对外的接口（MQ 消息）**：

```
接收队列：
  ← NavigatorCmd: PLAN_ROUTE {carId, start{x,y}, target{x,y}, algorithm, tick}

发送队列：
  → ControllerCmd: ROUTE_PLANNED {carId, routeFound, routeLength}

黑板写入：
  → CarID:RouteList (LPUSH 批量写入路径坐标 JSON)
```

> ⚠️ Navigator 只写 CarID:RouteList（领域数据），不写 CarID:Status

> 📌 **Navigator 多实例支持**（任务书要求）：Navigator 共享 `NavigatorCmd` Classic Queue（非独占），可同时启动多个 Navigator 进程竞争消费。RabbitMQ 默认轮询分发，天然实现"分解导航压力"。验收方式：`NavigatorMain` 多次启动，RabbitMQ 管理界面可看到多个消费者共享同一队列。

### 5.2 target-planner 模块交付清单

| 文件 | 职责 |
|------|------|
| `target-planner/TargetPlannerMain.java` | 入口：订阅 TargetPlannerCmd |
| `target-planner/GreedyTargetAllocator.java` | 贪心分配算法（读未探索区域 → 按曼哈顿距离排序 → 距离 ≥10 规则 → 写 CarID:Target） |

**TargetPlanner 对外的接口（MQ 消息）**：

```
接收队列：
  ← TargetPlannerCmd: ASSIGN_TARGET {carId, tick}

发送队列：
  → ControllerCmd: TARGET_ASSIGNED {carId, target{x,y}, success}
```

> ⚠️ TargetPlanner 只写 CarID:Target（领域数据），不写 CarID:Status

### 5.3 task-configurator 模块交付清单

| 文件 | 职责 |
|------|------|
| `task-configurator/TaskConfiguratorMain.java` | 入口：订阅 TaskConfigCmd |
| `task-configurator/TaskInitializer.java` | 初始化流程：清空 Redis → 随机障碍物 → 分配小车初始位置 → 点亮初始区域 → 写 TaskConfig |

**TaskConfigurator 对外的接口（MQ 消息）**：

```
接收队列：
  ← TaskConfigCmd: FORWARD_CONFIG {mapWidth, mapHeight, carCount, obstacleRatio, algorithm, tickInterval}
                   FORWARD_RESET {}

发送队列：
  → ControllerCmd: TASK_READY {}

黑板写入：
  → mapView, mapBlock, CarID:Position, CarID:Status, CarID:Steps, TaskConfig
```

### 5.4 动态障碍物模块

| 文件 | 职责 |
|------|------|
| `common/DynamicObstacleUtil.java` | 动态障碍物工具类（放 common，供 Controller 调用）：每 N tick 随机增删 mapBlock 障碍物，避开小车占位 |

> 📌 放在 common 而非 controller，因为只操作 `mapBlock` bitmap + `BlackboardClient`，无外部依赖，Person C 独立实现。Controller 在 tick() 中每 N 拍调用一次。

### 5.5 开发顺序

1. TaskConfiguratorMain + TaskInitializer（先跑通初始化流程）
2. TargetPlannerMain + GreedyTargetAllocator（贪心目标分配）
3. NavigatorMain + 消息收发
4. BfsPathFinder（先实现，可联调）
5. AStarPathFinder + PathPlannerFactory
6. DynamicObstacleUtil（放 common，Controller 集成）
7. 与 Person A 的 Controller 联调

---

## 六、Person D：display + launcher

### 为什么这样分配

display 是唯一涉及 Web 前端的模块，技术栈与前三人完全不同（Java → HTML/CSS/JS），拉出来独立负责。launcher 是集成模块，依赖所有人的模块，适合由 display 负责人在最后阶段汇总。

### 6.1 display 模块交付清单

| 文件 | 职责 |
|------|------|
| `display/DisplayMain.java` | 入口：启动 HTTP 服务 + WebSocket 服务 |
| `display/WebSocketBridge.java` | 订阅 UpdateView Fanout → 读黑板 → JSON 序列化 → 推浏览器；接收浏览器 WebSocket 消息 → 转发到 ControllerCmd |
| `display/HttpFileServer.java` | 提供 `display/src/main/resources/web/` 下的静态文件 |
| `display/web/index.html` | 前端主页 |
| `display/web/css/style.css` | 暗色工业风样式 |
| `display/web/js/app.js` | WebSocket 客户端 + Canvas 渲染（含热力图模式） + 控制面板逻辑 + 回放模式 + 步数排行榜 |

**Display 附加功能**（阶段3实现）：

| 功能 | 实现要点 |
|------|----------|
| 耗时统计 | 全局信息区显示"⏱ 耗时 XX 秒"，数据来自 REFRESH_ALL 消息 |
| 步数排行榜 | 每节拍读所有车步数降序排列渲染，Top 1 高亮 |
| 暂停/继续按钮 | 点击发 TOGGLE_PAUSE，按钮文字随 paused 状态切换 |
| 节拍速度滑块 | 滑块值域 100ms~2000ms，拖动发 SET_TICK_INTERVAL |
| 路径回放 | 回放开关 + 进度条 + 播放速度控制；读取 CarID:History 逐帧渲染 |
| 多算法对比 | 算法下拉框 + 对比结果表格（总步数/耗时/路径长度）（选做） |
| 热力图模式 | 切换开关（选做），按 mapHeat 计数器（HGETALL Hash）渲染色调映射（蓝→黄→红） |

> 📌 **Display 多实例支持**（任务书要求）：UpdateView 是 Fanout Exchange，天然支持多个 Display 进程各自绑定独占队列，同时接收广播并独立推送。验收方式：启动 2 个 DisplayMain 进程（不同端口如 8887、8888），分别打开浏览器，两个页面同时显示系统状态且互不干扰。

**Display 对外的接口**：

```
MQ 接收：
  ← UpdateView(fanout): REFRESH_ALL {tick, explorationRate}

MQ 发送：
  → ControllerCmd: SET_CONFIG {mapWidth, mapHeight, carCount, obstacleRatio, algorithm, tickInterval}
  → ControllerCmd: RESET {}

黑板读取（只读）：
  ← mapView, mapBlock, CarID:Position, CarID:Status, CarID:Steps, CarID:RouteList, CarID:Target, TaskConfig

WebSocket 推浏览器：
  → {type:"REFRESH_ALL", tick, explorationRate, taskConfig, cars:[...], mapView, mapBlock}

WebSocket 收浏览器：
  ← {type:"SET_CONFIG", ...} / {type:"RESET"}
```

### 6.2 前端渲染方案

**Canvas 分层渲染**：

```
层1: 地图背景（30×30 网格）
层2: 已探索区域（mapView bitmap → 点亮色填充）
层3: 障碍物（mapBlock bitmap → 红色方块）
层4: 规划路径（CarID:RouteList → 半透明线段）
层5: 小车（CarID:Position → 圆形 + 编号 + 状态颜色外圈）
```

**控制面板功能**：

| 组件 | 功能 |
|------|------|
| 配置表单 | 地图尺寸、小车数量、障碍物比例、算法选择、节拍间隔 |
| 按钮组 | 开始（发送 SET_CONFIG）/ 暂停 / 重置（发送 RESET） |
| 全局信息 | 节拍号、探索率百分比 + 进度条、运行时长 |
| 车辆状态卡 | 每台车：编号、状态（颜色标签）、当前位置、目标位置、步数 |
| 图例 | 5 种状态的颜色对照 |

> ⚠️ **动态车辆编号**：小车编号从 `TaskConfig.carCount` 动态生成，前端遍历黑板返回的 `cars` 数组渲染，不预设 5 台的固定数组。Canvas 小车渲染和状态卡列表均应动态适配车辆数量。

### 6.3 launcher 模块交付清单

| 文件 | 职责 |
|------|------|
| `launcher/LauncherMain.java` | 按顺序启动所有模块（独立线程） |

启动顺序：
```
1. TaskConfigurator  → 等待就绪
2. Navigator         → 等待就绪
3. TargetPlanner     → 等待就绪
4. Car × carCount     → 等待就绪
5. Display           → 等待就绪
6. Controller        → 最后启动，开始 tick
```

### 6.4 开发顺序

1. DisplayMain + HttpFileServer + WebSocketBridge 骨架
2. 前端静态页面（HTML + CSS）
3. 前端 Canvas 渲染（js/app.js）
4. 前端控制面板逻辑
5. WebSocketBridge 黑板读取 + JSON 组装
6. LauncherMain（等其他模块就绪后联调）
7. 与所有人联调

---

## 七、协作时间线

```
第1天（阶段1前半：A优先交付核心类）
  Person A: ━ BlackboardClient + MessageBus ━━━━━━┓
  Person B:   ━ Maven 骨架 + CarMain 空壳 + 学 Jedis API ━
  Person C:   ━ Maven 骨架 + NavMain 空壳 + 学 RabbitMQ API ━
  Person D:   ━ Maven 骨架 + DisplayMain 空壳 + 前端 HTML/CSS 静态页面 ━
                                                   ┃
第2天（阶段1后半：common收尾 + 各模块动工）         ┃
  Person A: ━ common 全部完成 ━━━━━━━━━━━━━━━━━━━━
  Person B:   ━ CarMain MQ 收发（用 A 上午交付的 MessageBus）━━
  Person C:   ━ TaskConfigurator 初始化流程 ━━━━━
  Person D:   ━ WebSocketBridge 骨架（依赖 MessageBus）━━

第3-4天（阶段2a：核心骨架）
  Person A: ━━ controller 开发 ━━━━━━━━
  Person B:               ━━ car 单步移动（不加锁）━━
  Person C:               ━━ task-configurator + target-planner ━━━━━
  Person D:   ━━ display 静态页面 + WebSocket 骨架 ━

第5-6天（阶段2b：算法+锁完善）
  Person A: ━━ controller 调度完善 ━━━
  Person B:               ━━ 分布式锁 ━━━━━━━━━
  Person C:               ━━ navigator BFS + DynamicObstacleUtil ━━
  Person D:   ━━ Canvas 渲染 + 控制面板 ━━━━━━━━━━━━

第7-9天（阶段3：集成联调 + 附加功能）
  Person A: ━━ 暂停/继续 + 节拍调速 + 动态障碍物 + 耗时统计 + 协调联调 ━━
  Person B:   ━━ Car History + mapHeat ━━━━ (配合 D 联调) ━
  Person C:   ━━ A* 补充 + 多实例验证 ━━━━━━━
  Person D:   ━━ 路径回放 + 步数排行 + 节拍滑块 + 暂停按钮 ━━ (热力图+算法对比选做)
  全员:     ━━━━━━━━━━━━━━ 联调 + bugfix ━━━━━━━━━━━━━━━

第10-11天（阶段4：测试+文档）
  全员:                    ━━━━━━━━━━ 测试 + 文档 ━━━━━━━
```

---

## 八、各模块间的接口契约

以下是每个人都必须遵守的、跨模块的接口约定。写完 common 后，这些由 common 模块的类型和常量保证。

### 8.1 Redis Key 命名规范

| Key 模板 | 类型 | 写入者 | 读取者 |
|----------|------|--------|--------|
| `mapView` | bitmap | Car, TaskConfigurator | 所有人 |
| `mapBlock` | bitmap | Car, TaskConfigurator | 所有人 |
| `{carId}:Position` | hash `{x, y}` | Car, TaskConfigurator | 所有人 |
| `{carId}:Target` | hash `{x, y}`（与 Position 一致） | TargetPlanner (HSET), Controller (DEL) | 所有人 |
| `{carId}:RouteList` | list (JSON string) | Navigator (LPUSH), Car (RPOP) | 所有人 |
| `{carId}:History` | list (JSON string) | Car (RPUSH 每步位置+tick) | Display（回放模式） |
| `{carId}:Status` | string | Controller, Car (各自管辖的变迁) | 所有人 |
| `{carId}:Steps` | string (int) | Car | 所有人 |
| `{carId}:BlockedTick` | string (int) — 不存在时 get 返回 -1 | Car (写), Controller (读/DEL 删) | Controller, Display |
| `TaskConfig` | hash | TaskConfigurator, Controller（elapsedSeconds, tickInterval） | 所有人 |
| `mapHeat` | hash field=`"{row},{col}"`（HINCRBY 计数器） | Car（HINCRBY 每个点亮格子） | Display（HGETALL 热力图模式） |
| `controller:instance` | string (SETNX) | Controller（单实例互斥） | — |
| `lock:{carId}` | string (SETNX) | Car (加锁), — (自动过期) | — |

### 8.2 Car 编号约定

车辆 ID 格式为 `Car` + 3 位数字（如 `Car001`）。Controller 和 TargetPlanner 通过 Redis `KEYS Car*:Status` 动态获取当前已注册车辆列表，而非从 `carCount` 硬推导。这确保了新 CarMain 进程启动后自动加入，无需修改配置。

```java
// Controller 动态发现已注册车辆
Set<String> carIds = jedis.keys("Car*:Status").stream()
    .map(key -> key.replace(":Status", ""))
    .collect(Collectors.toSet());
```

前端显示编号从 carId 提取数字部分（`Car001` → `001`），不预设数量。

### 8.3 消息格式统一规范

所有 MQ 消息 body 为 fastjson2 序列化的 JSON，结构：

```json
{
  "type": "消息类型",
  "tick": 0,
  "carId": "Car001",
  "timestamp": 1717400000000,
  "data": { }
}
```

### 8.4 状态写入权限矩阵

| 状态变迁 | 写入者 | 触发条件 |
|----------|--------|----------|
| → IDLE | Car | 初始化注册 / 路径走完 / 受阻超时后 Controller 写入 |
| → IDLE | Controller | BLOCKED 超时 ≥ 2 节拍 |
| → WAITING_ROUTE | Controller | 收到 TARGET_ASSIGNED，carId 在 pendingTargetRequests 中且 target 非空 |
| → READY | Controller | 收到 ROUTE_PLANNED 且 routeFound=true |
| → READY | Car | 移动一步完成，路径非空 |
| → MOVING | Car | 收到 TICK_MOVE，下一步无障碍 |
| → BLOCKED | Car | 下一步有障碍 |

### 8.5 队列名称常量

```java
public class QueueNames {
    public static final String CAR_PREFIX = "Car_";
    public static final String NAVIGATOR_CMD = "NavigatorCmd";
    public static final String TARGET_PLANNER_CMD = "TargetPlannerCmd";
    public static final String TASK_CONFIG_CMD = "TaskConfigCmd";
    public static final String CONTROLLER_CMD = "ControllerCmd";
    public static final String UPDATE_VIEW_EXCHANGE = "UpdateView";
    
    public static String carQueue(String carId) { return CAR_PREFIX + carId; }
}
```

### 8.6 分布式锁约定

```java
// 锁 key 格式
String lockKey = "lock:" + carId;

// 加锁：SETNX + 超时 5 秒
boolean acquired = lock.tryLock(5000);

// 释放：Lua 脚本验 ownership
lock.unlock();

// Car 移动期间的复合操作必须加锁
// Controller 写 Status 变迁前应尝试加锁（乐观锁 WATCH 或直接 tryLock 短超时）
```

---

## 九、测试与文档责任分配

| 任务 | 负责人 | 说明 |
|------|--------|------|
| 单元测试 | 各自 | 每人覆盖自己模块的核心逻辑（见开发计划 §5.1） |
| 集成测试 | Person A（协调） | 全员参与联调，A 负责组织、记录问题、追踪闭环 |
| 实验报告 | Person D（主笔） | D 的前端模块后期工作量下降，适合汇总；各人交付自己模块的设计文档章节（架构说明、核心类设计、接口定义） |

---

## 十、沟通与协作规则

| 规则 | 说明 |
|------|------|
| common 接口定稿 | **common 接口设计（BlackboardClient 方法签名、MessageBus 方法签名、消息格式）在第 1 天完成初版后，全员 review 确认方可定稿** |
| common 接口变更 | 变更必须先群内说明动机和影响范围，获全员确认后再改 |
| Redis key 增删 | 必须全员同意，更新本文件 §8.1 |
| MQ 消息格式变更 | 必须全员同意，更新本文件 §8.3 |
| 每个人的模块 | 独立进程，自己写 Main 入口，自己加单元测试 |
| 代码仓库 | 分支命名 `feature/common`、`feature/controller`、`feature/car`、`feature/navigator`、`feature/display` 等；common 先合入 main → 其他人 rebase main 后再开发；提 PR 前必须本地 `mvn test` 通过 |
| 每日对齐 | 收工前 15 分钟，各自说进度 + 阻塞项 |
| 互为 backup | 同组另外三人中至少一人能协助排查对方模块的问题 |
| 仓库地址 | **【待填写】** Git 仓库 URL |

---

## 十一、风险预案

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| common 延期 | 中 | 阻塞所有人 | B/C/D 第 1 天不等待，直接开工：搭建 Maven 模块骨架、定义 Main 入口空壳、学习 Jedis/RabbitMQ API、Person D 写前端静态页面（不依赖 Java 后端）；A 优先交付 BlackboardClient + MessageBus 核心 2 类，其余辅助类可延后 |
| 某人模块卡住 | 中 | 该模块进度滞后 | 提前约定"互为 backup"（见 §十）；先推进模块的降级版本（见开发计划 §八 降级路径），先跑通再完善 |
| Redis/RabbitMQ 环境问题 | 低 | 无法联调 | 统一使用 docker-compose.yml 一键启动（见开发计划 §0.2），禁止各人自行安装不同版本 |
| Controller 异步调度 bug | 中 | 车状态卡死 | 优先用状态日志验证（每 tick 打印所有车状态）；加单元测试覆盖 5 种状态分支 |
| 联调时接口不匹配 | 中 | 联调阻塞 | common 消息格式初版定稿后不再改；联调前各自用单元测试 mock MQ 消息验证 |
